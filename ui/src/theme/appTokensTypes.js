/**
 * Semantic design tokens attached to MUI theme as `theme.appTokens`.
 *
 * @typedef {object} AppTokens
 * @property {{ app: string, sidebar: string, panel: string, panelAlt: string, code: string, subtle: string }} surface
 * @property {{ default: string, strong: string }} border
 * @property {{ primary: string, secondary: string, muted: string, accent: string, faint: string }} text
 * @property {{
 *   softBg: string,
 *   softBorder: string,
 *   fg: string,
 *   emphasisHoverBg: string,
 *   emphasisHoverBorder: string,
 *   chipReadyBg: string,
 *   chipReadyFg: string,
 * }} accent
 * @property {{
 *   dangerFg: string,
 *   dangerBg: string,
 *   dangerBorder: string,
 *   dangerHoverBorder: string,
 * }} state
 * @property {{
 *   outlinedBg: string,
 *   outlinedBgHover: string,
 *   outlinedBorderHover: string,
 *   navItemHoverBg: string,
 *   chipMutedBg: string,
 *   chipMutedFg: string,
 * }} control
 */

export {};
