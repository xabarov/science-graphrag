/** Workspace shell (paper list); not a tool tab host in the canonical IA. */
export const WORKSPACE_PATH = "/workspace";

/** Primary chat route (replaces legacy `/ask` in UI and deep links). */
export const CHAT_PATH = "/chat";

/** Standalone reader tool (workspace shell is paper list only). */
export const READER_PATH = "/reader";

/** Standalone graph tool. */
export const GRAPH_PATH = "/graph";

/** Standalone chunk-level evidence inspection. */
export const EVIDENCE_PATH = "/evidence";

/** Legacy ask route — kept for redirects only. */
export const LEGACY_ASK_PATH = "/ask";
